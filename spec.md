あなたは、React、TypeScript、Vite、地図Webアプリ、GitHub Pagesに詳しいシニアフロントエンドエンジニアです。



以下の仕様をすべて満たすWebアプリを、実際に動作する完全なソースコードとして作成してください。



質問を返さず、合理的な初期値を採用して実装を完了してください。

仕様に書かれていない大規模な機能を勝手に追加しないでください。

疑問がある場合は、複雑な方式ではなく、GitHub Pages上で安定して動く単純な方式を選択してください。



\# 1. アプリの概要



アプリ名：



とやま名水めぐり



目的：



富山県内の名水スポットを地図で探し、現地を訪問してGPSによるデジタルスタンプを獲得する、スマートフォン向け観光Webアプリを作成する。



公開先：



GitHub Pages



利用端末：



スマートフォンを最優先とする。

PCやタブレットでも閲覧できるレスポンシブデザインとする。



対象言語：



日本語



\# 2. 使用するオープンデータ



富山県オープンデータポータルサイトの「とやまの名水一覧」を使用する。



データセット：



\[とやまの名水一覧](https://ckan.tdcp.pref.toyama.jp/dataset/meisui/resource/3e98a397-8090-48bc-8a7e-0225e2834a41)



重要事項：



1\. GitHub Pagesは静的ホスティングなので、ブラウザから外部XLSXを毎回直接取得する構成にはしない。

2\. CORS、リンク変更、XLSX解析失敗の影響を避ける。

3\. アプリで利用するデータは、リポジトリ内のJSONファイルとして保持する。

4\. 配置先は public/data/meisui.json とする。

5\. public/data/meisui.json をfetchで読み込む。

6\. XLSXをJSONへ変換するNode.jsスクリプト scripts/convert-xlsx.mjs も作成する。

7\. 元データの列名が想定と異なる場合に備え、変換スクリプトでは列名候補を配列で定義する。

8\. データ変換に失敗した行は無視せず、警告としてコンソールに出す。

9\. 元データに緯度・経度が存在しない場合は、架空の緯度・経度を生成しない。

10\. 緯度・経度がないスポットは一覧には表示し、地図とGPSチェックインの対象外とする。

11\. 開発確認用として、少なくとも5件のサンプルデータを public/data/meisui.json に含める。

12\. サンプルデータであることをREADMEに明記する。

13\. 実在地点について、知識だけで緯度・経度、説明、住所を補完しない。



\# 3. 技術構成



以下を使用する。



\- React

\- TypeScript

\- Vite

\- React Router

\- Leaflet

\- react-leaflet

\- OpenStreetMapの地図タイル

\- CSS Modulesまたは通常のCSS

\- localStorage

\- Vitest

\- ESLint

\- GitHub Actions



使用しないもの：



\- Next.js

\- Nuxt

\- サーバーサイドレンダリング

\- PHP

\- Pythonバックエンド

\- Node.jsバックエンド

\- データベース

\- Firebase

\- Supabase

\- AWS

\- 外部認証

\- 有料API

\- Google Maps API

\- APIキーが必要なサービス

\- Tailwind CSS

\- UIコンポーネントライブラリ

\- 外部LLM API



理由：



GitHub Pagesだけで動作する、完全な静的Webアプリにするため。



\# 4. GitHub Pages対応



GitHub Pagesのプロジェクトサイトとして公開できるようにする。



例：



https://ユーザー名.github.io/リポジトリ名/



対応要件：



1\. vite.config.ts の base は固定値にしない。

2\. GitHub Actionsでビルドする場合は、環境変数からリポジトリ名を取得するか、相対パスで正しく動く方法を採用する。

3\. ローカル開発時は base を "/" とする。

4\. GitHub Actions上ではリポジトリ名を使用して "/リポジトリ名/" とする。

5\. GitHubのユーザーサイトの場合も考慮し、リポジトリ名が「ユーザー名.github.io」のときは base を "/" とする。

6\. BrowserRouterではなくHashRouterを使用する。

7\. ページを直接再読み込みしても404にならないようにする。

8\. public内のデータ参照では "/data/meisui.json" のようなルート固定パスを使用しない。

9\. import.meta.env.BASE\_URL を使用してデータURLを生成する。

10\. .github/workflows/deploy.yml を作成する。

11\. mainブランチへのpushで自動デプロイする。

12\. workflow\_dispatchによる手動実行にも対応する。

13\. npm ci、npm run test、npm run buildの順で実行する。

14\. GitHub Pagesへの公開には actions/configure-pages、actions/upload-pages-artifact、actions/deploy-pagesを使用する。

15\. permissionsとしてcontents: read、pages: write、id-token: writeを設定する。

16\. distディレクトリを公開対象とする。

17\. Node.jsのバージョンはLTSを使用する。

18\. package-lock.jsonを生成可能な構成にする。

19\. npm run buildでTypeScriptの型チェックとViteビルドが実行されるようにする。



\# 5. データ型



以下を基本型とする。



type MeisuiSpot = {

&#x20; id: string;

&#x20; name: string;

&#x20; municipality: string;

&#x20; address: string;

&#x20; description: string;

&#x20; latitude: number | null;

&#x20; longitude: number | null;

&#x20; imageUrl: string | null;

&#x20; sourceUrl: string | null;

&#x20; notes: string | null;

};



必須ルール：



1\. idは重複しない文字列とする。

2\. 緯度は-90以上90以下とする。

3\. 経度は-180以上180以下とする。

4\. 空欄をundefinedのまま扱わず、文字列項目は空文字、任意項目はnullに正規化する。

5\. JSONを読み込んだ後、型と値を検証する。

6\. 不正なデータが一部含まれていても、アプリ全体を停止させない。

7\. 読み込みに失敗した場合は、利用者向けのエラー画面と再読み込みボタンを表示する。

8\. データの出典を画面内に表示する。



\# 6. MVPの機能範囲



今回実装する機能は次に限定する。



\## 6.1 ホーム画面



表示内容：



\- アプリ名

\- 短い説明

\- 総スポット数

\- 訪問済みスポット数

\- 達成率

\- 「地図から探す」ボタン

\- 「スタンプ帳を見る」ボタン

\- 現在地付近のスポット最大3件

\- データ出典

\- 位置情報に関する説明



達成率の計算：



訪問済みスポット数 ÷ 全スポット数 × 100



小数点以下は四捨五入する。

スポット数が0件の場合は0%とする。



\## 6.2 地図画面



表示内容：



\- 富山県周辺を初期表示

\- 名水スポットのマーカー

\- 訪問済みと未訪問を色またはアイコンで区別

\- 現在地表示ボタン

\- 一覧表示への切り替え

\- 市町村による絞り込み

\- 名称、住所、市町村を対象にした検索

\- 名水詳細画面へのリンク



緯度・経度がnullのスポットは地図に表示しない。

現在地が取得できなくても、通常の地図閲覧はできるようにする。



LeafletのマーカーアイコンがViteビルド後に表示されない問題を避けるため、アイコン画像の参照方法を明示的に設定する。



地図タイルには適切なOpenStreetMap帰属表示を必ず表示する。



\## 6.3 スポット一覧画面



表示内容：



\- スポット名

\- 市町村

\- 住所

\- 訪問済み状態

\- 現在地が取得済みの場合は現在地からの直線距離

\- 詳細画面へのリンク



機能：



\- 名称検索

\- 市町村絞り込み

\- 未訪問のみ表示

\- 現在地から近い順

\- 名前順



現在地が取得できていない場合、「現在地から近い順」は選択不可または説明を表示する。



\## 6.4 名水詳細画面



表示内容：



\- スポット名

\- 市町村

\- 住所

\- 説明

\- 注意事項

\- 訪問済み状態

\- 緯度・経度

\- 現在地からの直線距離

\- 地図

\- 「スタンプを取得する」ボタン

\- 「外部地図で経路を見る」リンク

\- データ出典



sourceUrlがある場合のみ、出典リンクを表示する。

imageUrlがある場合のみ画像を表示する。

画像がない場合は、CSSで作成した水をイメージするプレースホルダーを表示する。



外部地図リンクには緯度・経度を使用する。

緯度・経度がない場合は外部地図リンクを表示しない。



\## 6.5 GPSスタンプ取得



スタンプ取得条件：



1\. スポットに有効な緯度・経度がある。

2\. ブラウザがGeolocation APIに対応している。

3\. 利用者が位置情報の使用を許可している。

4\. 利用者とスポットの直線距離が100メートル以内である。

5\. 位置情報のaccuracyが200メートル以下である。



距離計算：



Haversine公式を使用する。

距離はメートル単位で計算する。

計算関数をsrc/utils/distance.tsとして分離する。



Geolocation APIの設定：



\- enableHighAccuracy: true

\- timeout: 10000

\- maximumAge: 30000



成功時：



\- スタンプ獲得画面を表示する。

\- localStorageへ保存する。

\- 獲得日時を保存する。

\- 同じスポットを再度取得しても重複登録しない。

\- 「スタンプを獲得しました」と明確に表示する。



失敗時のメッセージ：



\- 位置情報が許可されていない

\- 位置情報を取得できない

\- GPS精度が不足している

\- スポットから遠すぎる

\- スポットに位置情報が登録されていない

\- ブラウザが位置情報取得に対応していない

\- タイムアウトした



単に「エラー」と表示せず、利用者が次に取るべき行動を日本語で説明する。



例：



「現在地とスポットの距離は約235mです。スポットから100m以内で、もう一度お試しください。」



\## 6.6 スタンプ帳画面



表示内容：



\- 全体の達成率

\- 訪問済み数

\- 未訪問数

\- 市町村別の達成状況

\- 各スポットの訪問済み状態

\- スタンプ獲得日時

\- バッジ一覧



バッジ条件：



\- はじめの一滴：1か所訪問

\- 名水ビギナー：5か所訪問

\- 名水めぐり人：15か所訪問

\- 富山名水マスター：全スポット訪問



データ件数が5件未満でも「名水ビギナー」は5件訪問を条件とする。

全スポット数が0件の場合、「富山名水マスター」を獲得扱いにしない。



\## 6.7 設定画面



機能：



\- 利用方法表示

\- 位置情報の利用理由

\- データ出典

\- オープンデータに関する表示

\- プライバシー説明

\- 保存データの初期化

\- アプリのバージョン表示



初期化ボタンは誤操作を防ぐため、確認ダイアログを表示する。



\# 7. localStorage



キー名にはアプリ固有の接頭辞を付ける。



例：



toyama-meisui:visits



保存形式：



type VisitRecord = {

&#x20; spotId: string;

&#x20; visitedAt: string;

};



visitedAtはISO 8601形式とする。



要件：



1\. localStorageの値を直接信用しない。

2\. JSON.parseをtry-catchで囲む。

3\. 不正なデータの場合は空配列として扱う。

4\. スポットIDと獲得日時を検証する。

5\. 同じspotIdは1件だけ保存する。

6\. 保存に失敗した場合は利用者に通知する。

7\. 個人名、メールアドレス、正確な現在地、移動履歴は保存しない。

8\. GPSで取得した緯度・経度は保存しない。

9\. サーバーへの送信は行わない。



\# 8. 画面遷移



HashRouterを使用し、次のルートを実装する。



\- /             ホーム

\- /map          地図

\- /spots        スポット一覧

\- /spots/:id    スポット詳細

\- /stamps       スタンプ帳

\- /settings     設定

\- \*             404画面



404画面にはホームへ戻るボタンを表示する。



画面下部にはスマートフォン向けの固定ボトムナビゲーションを表示する。



ナビゲーション項目：



\- ホーム

\- 地図

\- 一覧

\- スタンプ帳

\- 設定



現在表示中の項目が分かるようにする。



\# 9. デザイン



テーマ：



富山の清らかな水、立山、自然を連想するデザイン。



色：



\- メイン：深い青

\- サブ：水色

\- アクセント：緑

\- 背景：白またはごく薄い青

\- エラー：赤

\- 警告：黄またはオレンジ



要件：



1\. スマートフォンの幅320pxでも横スクロールを発生させない。

2\. タップ領域は最低44pxを目安にする。

3\. 本文文字は16pxを基本とする。

4\. 色だけで状態を表現しない。

5\. 訪問済みにはチェックマークとテキストを表示する。

6\. コントラストを確保する。

7\. キーボード操作に対応する。

8\. フォーカス表示を消さない。

9\. buttonとaを用途に応じて正しく使い分ける。

10\. aria-labelを必要な箇所に付ける。

11\. 見出しレベルを順番どおりにする。

12\. prefers-reduced-motionを考慮する。

13\. 不要なアニメーションを使わない。

14\. ローディング状態を表示する。

15\. 空データ状態を表示する。



\# 10. コンポーネント構成



最低限、次のように分割する。



src/

&#x20; components/

&#x20;   AppHeader.tsx

&#x20;   BottomNavigation.tsx

&#x20;   LoadingView.tsx

&#x20;   ErrorView.tsx

&#x20;   EmptyView.tsx

&#x20;   ProgressCard.tsx

&#x20;   SpotCard.tsx

&#x20;   SpotMarker.tsx

&#x20;   MunicipalityFilter.tsx

&#x20;   SearchBox.tsx

&#x20;   StampBadge.tsx

&#x20; hooks/

&#x20;   useMeisuiData.ts

&#x20;   useGeolocation.ts

&#x20;   useVisits.ts

&#x20; pages/

&#x20;   HomePage.tsx

&#x20;   MapPage.tsx

&#x20;   SpotListPage.tsx

&#x20;   SpotDetailPage.tsx

&#x20;   StampBookPage.tsx

&#x20;   SettingsPage.tsx

&#x20;   NotFoundPage.tsx

&#x20; types/

&#x20;   meisui.ts

&#x20; utils/

&#x20;   distance.ts

&#x20;   storage.ts

&#x20;   validation.ts

&#x20; styles/

&#x20;   global.css

&#x20; App.tsx

&#x20; main.tsx



必要に応じた追加ファイルは作成してよい。

ただし、1ファイルに全コードをまとめない。



\# 11. PWA対応



可能な範囲でインストール可能なPWAにする。



要件：



\- manifest.webmanifestを用意する。

\- アプリ名、短縮名、テーマカラー、背景色を定義する。

\- displayはstandaloneとする。

\- 192pxと512pxのアイコンを用意する。

\- アイコンは著作権上問題のない単純な水滴モチーフとする。

\- プログラムで生成できない場合はSVGアイコンを作成する。

\- アプリ本体の更新を妨げる複雑なService Workerは実装しない。

\- 完全なオフライン動作を今回の必須条件にしない。



\# 12. テスト



Vitestを使用する。



最低限、次をテストする。



1\. 同じ座標間の距離がほぼ0mになる。

2\. Haversine距離計算が妥当な値を返す。

3\. 緯度・経度nullのデータをGPS判定対象にしない。

4\. 距離100m以内をチェックイン可能と判定する。

5\. 100mを超える場合をチェックイン不可と判定する。

6\. accuracyが200mを超える場合をチェックイン不可と判定する。

7\. 訪問記録が重複しない。

8\. 壊れたlocalStorageデータを安全に処理する。

9\. 全スポット数0件の達成率が0%になる。

10\. 全スポット数0件で完全制覇バッジを付与しない。



テストしやすいように、距離判定、達成率、バッジ判定、localStorage処理を純粋関数として分離する。



\# 13. エラー処理



次のケースでアプリが白画面にならないようにする。



\- JSONファイルが存在しない

\- JSONが不正

\- データが0件

\- 一部のデータが不正

\- 位置情報が拒否された

\- 位置情報取得がタイムアウトした

\- Leafletの地図読み込みに失敗した

\- localStorageが利用できない

\- 存在しないスポットIDへアクセスした

\- 外部画像が読み込めない



console.errorだけで終わらせず、必要なケースでは利用者向けメッセージを表示する。



\# 14. セキュリティとプライバシー



1\. innerHTMLを使わない。

2\. dangerouslySetInnerHTMLを使わない。

3\. データ中の文字列をHTMLとして解釈しない。

4\. 外部リンクにはrel="noopener noreferrer"を付ける。

5\. 外部リンクが新しいタブで開く場合は、そのことが分かる表示を行う。

6\. 現在地を外部サーバーへ送信しない。

7\. GPS座標をlocalStorageへ保存しない。

8\. アクセス解析を実装しない。

9\. Cookieを使用しない。

10\. 秘密情報やAPIキーをコードへ含めない。

11\. サンプルデータと実データを明確に区別する。

12\. 「飲用可能」とデータに明記されていない場合、飲用可能と表示しない。

13\. 水質、安全性、災害時利用について根拠のない表現をしない。

14\. 現地の案内、立入制限、管理者の指示を優先する注意書きを表示する。



\# 15. 今回実装しない機能



次の機能は、画面だけのダミーも作成しない。



\- ユーザー登録

\- ログイン

\- SNSログイン

\- サーバー同期

\- ランキング

\- 写真投稿

\- コメント

\- QRコードチェックイン

\- 景品応募

\- クーポン

\- プッシュ通知

\- AIによる観光提案

\- GTFS経路検索

\- 管理画面

\- アクセス解析

\- 他の利用者との共有

\- 不正利用の完全な防止



READMEの将来拡張候補として記載するだけにする。



\# 16. README



README.mdに次を記載する。



\- アプリの目的

\- 主な機能

\- 使用技術

\- ディレクトリ構成

\- セットアップ方法

\- npm install

\- npm run dev

\- npm run test

\- npm run build

\- npm run preview

\- GitHub Pagesへの公開方法

\- GitHubのSettingsからPagesのSourceをGitHub Actionsにする手順

\- オープンデータの出典

\- サンプルデータ利用時の注意

\- XLSXからJSONへ変換する方法

\- 対応ブラウザ

\- 位置情報はHTTPSまたはlocalhostで利用する必要があること

\- 保存される情報

\- 保存されない情報

\- 制約事項

\- 将来拡張

\- ライセンス

\- OpenStreetMapの帰属表示

\- 飲用可否を保証しない旨

\- 現地情報を優先する旨



\# 17. データ変換スクリプト



scripts/convert-xlsx.mjsを作成する。



目的：



ローカルに保存したXLSXを public/data/meisui.json へ変換する。



実行例：



npm run convert-data -- ./data/meisui.xlsx



要件：



1\. xlsxパッケージを使用する。

2\. コマンドライン引数から入力ファイルを取得する。

3\. 入力ファイルがない場合は使い方を表示して終了コード1で終了する。

4\. 最初のワークシートを読み込む。

5\. 空行を除外する。

6\. 列名候補を定義する。



列名候補例：



\- 名称：名称、名水名、スポット名

\- 市町村：市町村、市町村名、所在地市町村

\- 住所：住所、所在地

\- 説明：説明、概要、紹介

\- 緯度：緯度、latitude、lat

\- 経度：経度、longitude、lng、lon

\- 画像：画像、画像URL、imageUrl

\- 出典：出典、URL、sourceUrl

\- 備考：備考、注意事項、notes



7\. 全角数字や余分な空白を可能な範囲で正規化する。

8\. 緯度・経度をnumberまたはnullに変換する。

9\. 範囲外の緯度・経度はnullにする。

10\. 緯度か経度の片方だけが有効な場合は両方nullにする。

11\. IDは元データに安定したIDがあれば利用する。

12\. IDがなければ、名称、市町村、住所を基に安定して再生成できるIDを作る。

13\. ID生成に配列の行番号だけを使用しない。

14\. 重複IDを検出する。

15\. 変換件数、警告件数、除外件数をコンソールへ出す。

16\. JSONはUTF-8、2スペースインデントで出力する。

17\. 出力ディレクトリがなければ作成する。

18\. 変換前のXLSXをリポジトリへ必須配置しない。

19\. 変換スクリプトの使い方をREADMEに記載する。



\# 18. package.jsonのスクリプト



最低限、次を定義する。



\- dev

\- build

\- preview

\- test

\- test:run

\- lint

\- convert-data



buildでは、TypeScriptの型チェック後にViteビルドを実行する。



例：



"build": "tsc -b \&\& vite build"



\# 19. 完了条件



以下をすべて満たした場合のみ完成とする。



\- npm installが成功する。

\- npm run lintが成功する。

\- npm run test:runが成功する。

\- npm run buildが成功する。

\- distが生成される。

\- GitHub PagesのサブパスでCSSとJavaScriptが読み込める。

\- public/data/meisui.jsonがビルド成果物に含まれる。

\- HashRouterで各画面を開ける。

\- 再読み込みで404にならない。

\- 地図が表示される。

\- マーカーアイコンが表示される。

\- データ一覧が表示される。

\- 絞り込みと検索が動作する。

\- GPS拒否時に適切な説明が表示される。

\- 100m以内でスタンプを保存できる。

\- GPS座標を保存しない。

\- 訪問記録が重複しない。

\- ブラウザ再起動後もスタンプが残る。

\- 全記録を設定画面から初期化できる。

\- スマートフォン表示で横スクロールしない。

\- TypeScriptのanyを安易に使用しない。

\- ESLintエラーを残さない。

\- READMEだけでセットアップと公開方法が分かる。



\# 20. 作業手順



次の順番で作業する。



1\. 要件を短く整理する。

2\. ファイル構成を決める。

3\. package.jsonと設定ファイルを作る。

4\. 型定義を作る。

5\. サンプルJSONを作る。

6\. データ検証処理を作る。

7\. 距離計算を作る。

8\. localStorage処理を作る。

9\. hooksを作る。

10\. 共通コンポーネントを作る。

11\. 各画面を作る。

12\. ルーティングを設定する。

13\. CSSを作る。

14\. テストを作る。

15\. データ変換スクリプトを作る。

16\. GitHub Actionsを作る。

17\. READMEを作る。

18\. npm run lintを実行する。

19\. npm run test:runを実行する。

20\. npm run buildを実行する。

21\. 発生したエラーを修正する。

22\. 完了条件を一つずつ確認する。



途中でビルドエラーやテストエラーが出た場合は、未解決のまま終了しない。

原因を調べ、修正してから再実行する。



\# 21. 出力方法



ファイル作成機能を利用できる場合：



\- 実際にすべてのファイルを作成する。

\- コードの説明だけで終了しない。

\- lint、test、buildを実行する。

\- 最後に変更したファイル一覧を示す。

\- 最後に検証結果を示す。



ファイル作成機能を利用できない場合：



1\. 最初にディレクトリ構成を示す。

2\. その後、すべてのファイルを省略せずに示す。

3\. 各コードブロックの直前にファイルパスを示す。

4\. 「以下同様」「省略」「必要に応じて追加」などでコードを省略しない。

5\. 最後に実行コマンドを示す。

6\. 最後にGitHub Pagesの設定手順を示す。



\# 22. 最終報告



実装完了後、次の形式で報告する。



\## 実装した機能

箇条書き



\## 技術構成

箇条書き



\## 作成・変更したファイル

箇条書き



\## 検証結果

\- npm run lint：成功または失敗

\- npm run test:run：成功または失敗

\- npm run build：成功または失敗



実際にコマンドを実行していない場合は「未実行」と明記する。

実行していないものを「成功」と書いてはいけない。



\## GitHub Pages公開手順

番号付き手順



\## 制約事項

箇条書き



\## 次の拡張候補

箇条書き



ここまでの仕様に従って、アプリを完成させてください。

